/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

/**
 * Any CSS included here will be global. The classic template
 * bundles Infima by default. Infima is a CSS framework designed to
 * work well for content-centric websites.
 */

/* You can override the default Infima variables here. */
:root {
  --ifm-color-primary: #ef4242;
  --ifm-color-primary-dark: #ed2626;
  --ifm-color-primary-darker: #eb1818;
  --ifm-color-primary-darkest: #c51111;
  --ifm-color-primary-light: #f15e5e;
  --ifm-color-primary-lighter: #f36c6c;
  --ifm-color-primary-lightest: #f69696;
  --ifm-code-font-size: 95%;
}

blockquote {
/*  background: #f9f9f9;
  border-left: 10px solid #ccc;
  margin: 1.5em 10px;
  padding: 0.5em 10px;*/
  font-style: italic;
  quotes: "\201C""\201C";
}
blockquote:before {
  color: #ccc;
  content: open-quote;
  font-size: 3em;
  line-height: 0.1em;
  margin-right: 0.25em;
  vertical-align: -0.4em;
}
blockquote p {
  display: inline;
}

/* Announcement banner */
div[class^="announcementBarContent"] {
  a {
    display: inline-block;
    color: var(--brand) !important;
    &:hover {
      color: var(--ifm-color-primary-darkest) !important;
    }
  }
}

.tooltip {
    position: relative;
    display: inline-block;
}

.tooltip .tooltiptext {
    visibility: hidden;
    width: 200px;
    background-color: black;
    color: #fff;
    text-align: center;
    border-radius: 6px;
    padding: 5px 10px;
    position: absolute;
    z-index: 1;
    bottom: 100%; /* Показывать подсказку сверху */
    left: 50%;
    margin-left: -100px; /* Центрировать подсказку относительно ссылки */
    opacity: 0;
    transition: opacity 0.3s;
}

.tooltip:hover .tooltiptext {
    visibility: visible;
    opacity: 1;
}
